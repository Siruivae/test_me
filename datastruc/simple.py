def simple(s):
    for i in range[len(s)]:
        min = s[i]
        for i in range[i+1, len[s]]:
            if min > s[j]:
                tmp = s[j]
                s[j] = min
                min = tmp
def s(l):
    for i in range(len(l)):
        min = l[i]
        for j in range(i+1, len(l)):
            if l[j] < min
                tmp = l[j]
                l[j] = min
                min = tmp
        l[i] = min